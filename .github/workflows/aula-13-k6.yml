name: Aula 13 - K6 Básico

on:
  workflow_dispatch:

jobs:
  k6-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Rodar teste de performance com K6
        uses: grafana/k6-action@v0.3.0
        with:
          filename: k6/aula-13-basic.js